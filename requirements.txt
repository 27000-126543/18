# ============================================
# 制造业预测性维护与备件智能管理系统 - 依赖
# Python 3.8+
# ============================================

# ---------- Web框架 (必须) ----------
Flask>=3.0.0
Werkzeug>=3.0.0
Jinja2>=3.1.0

# ---------- 数据导出 (运行时自动安装, 可选) ----------
openpyxl>=3.0.0
reportlab>=3.6.0

# ---------- 可选增强 (用于更高性能分析) ----------
# pandas>=1.3.0
# numpy>=1.21.0

# ---------- 说明 ----------
# 1. 导出Excel/PDF功能运行时会自动检测并安装openpyxl/reportlab
# 2. 核心功能仅需Flask即可运行，数据库使用Python内置SQLite3
# 3. 图表使用ECharts CDN引入，无需额外安装
